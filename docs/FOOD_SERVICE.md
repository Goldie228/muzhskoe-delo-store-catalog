
# 📦 Модуль доставки еды (Food Service)

## 📋 Обзор

Модуль `food_service` реализует API для управления каталогом блюд и категорий ресторана "Мужское дело".

## 🏗️ Архитектура

Модуль построен по паттерну MVC:
- **Services**: Бизнес-логика и работа с JSON-файлами (`DishesService`, `CategoriesService`).
- **Controllers**: Обработка HTTP-запросов и ответов.
- **Routes**: Определение эндпоинтов API.

## 🍔 Сущности

### 1. Dish (Блюдо)
- `name`: Название блюда (string)
- `price`: Цена (number)
- `inStock`: В наличии (boolean)
- `addedAt`: Дата добавления (Date)
- `ingredients`: Ингредиенты (Array<string>)

### 2. Category (Категория)
- `title`: Заголовок (string)
- `sortOrder`: Порядок сортировки (number)
- `isVisible`: Видимость (boolean)
- `lastModified`: Дата изменения (Date)
- `synonyms`: Синонимы для поиска (Array<string>)

## 🌐 Эндпоинты

### Блюда
- `GET /api/food` — Получить все блюда
- `GET /api/food/:id` — Получить блюдо по ID
- `POST /api/food` — Создать блюдо
- `PUT /api/food/:id` — Обновить блюдо
- `DELETE /api/food/:id` — Удалить блюдо
- `GET /api/food/search/price?min=X&max=Y` — Поиск по цене

### Категории
- `GET /api/categories` — Получить все категории
- `GET /api/categories/:id` — Получить категорию по ID
- `POST /api/categories` — Создать категорию
- `PUT /api/categories/:id` — Обновить категорию
- `DELETE /api/categories/:id` — Удалить категорию
- `GET /api/categories/visible` — Получить только видимые категории

## 💻 Примеры запросов

### Создание блюда
```bash
curl -X POST http://localhost:3000/api/food \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Паста Карбонара",
    "price": 550,
    "inStock": true,
    "ingredients": ["спагетти", "бекон", "сливки", "яйцо"]
  }'
```

### Поиск по цене
```bash
curl http://localhost:3000/api/food/search/price?min=100&max=600
```
