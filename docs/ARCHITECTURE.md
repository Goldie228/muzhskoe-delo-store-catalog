
# 🏗️ Архитектура фреймворка

## 📋 Обзор

Фреймворк "Мужское дело" представляет собой **Client-Server приложение** (SPA), в котором Frontend (браузерная часть) и Backend (Node.js часть) полностью разделены логически, но тесно взаимодействуют через RESTful API.

Архитектура разделена на два больших уровня:
1.  **Backend (Node.js)**: Самописный фреймворк `core/`, обеспечивающий обработку HTTP-запросов и управление модульной бизнес-логикой.
2.  **Frontend (Vanilla JS)**: Собственный клиентский роутер, модульные вьюхи и библиотека UI для управления DOM без использования фреймворков (React, Vue, jQuery).

---

## 🧩 Компоненты архитектуры (Backend)

### Ядро фреймворка (`core/`)

#### App.js (`core/App`)
Центральный класс приложения, управляющий жизненным циклом HTTP-сервера.
- **Управление Middleware:** Регистрирует и выполняет глобальные middleware (BodyParser, Auth, Static).
- **Маршрутизация:** Интегрирует `Router` для поиска обработчиков запросов.
- **Обработка Ошибок:** Предоставляет возможность установки кастомного обработчика через `setErrorHandler`.
- **Обертки:** Создает экземпляры `Request` и `Response` для каждого входящего запроса.

**Особенность:** В данной архитектуре все глобальные middleware (зарегистрированные через `app.use`) выполняются **перед** поиском маршрута. Middleware, отправляющий ответ без вызова `next()`, полностью останавливает дальнейшую обработку.

#### Router.js (`core/Router`)
Мощный маршрутизатор, не зависящий от контекста сервера.
- **Поддержка параметров:** `/users/:id`, `/files/:category/:id`.
- **Query-строки:** Автоматический парсинг `/search?q=test&page=2`.
- **Приоритеты:** Точные маршруты имеют приоритет над параметризованными.
- **Регулярные выражения:** Внутреннее использование RegExp для быстрого поиска.

#### Request.js & Response.js (`core/Request`, `core/Response`)
Обертки над нативными `IncomingMessage` и `ServerResponse`.
- **Request:** Обеспечивает удобный доступ к `req.params`, `req.query`, `req.body` (после парсинга).
- **Response:** Добавляет цепочки вызовов (chaining), методы `json()`, `send()`, `status()` и правильную установку заголовков.

#### Middleware (`core/middleware/`)

**bodyParser.js**
Middleware для автоматического парсинга тела запроса.
- Поддерживает `application/json`, `application/x-www-form-urlencoded`, `text/*`.
- Защищает от DoS-атак через ограничение размера тела (`limit`).
- Генерирует ошибки с кодом 413 при превышении лимита.

**errorHandler.js**
Фабрика для создания middleware обработки ошибок.
- В отличие от классического Express, где `errorHandler` регистрируется через `app.use(...)`, в нашем фреймворке он устанавливается специальным методом `app.setErrorHandler(...)`.
- Логирует ошибки с контекстом.
- Скрывает детализированные стеки вызовов в `production`, сохраняя их в `development`.

**static.js**
Middleware для обслуживания статических файлов (HTML, CSS, JS, IMG).
- Использует `fs.createReadStream` для эффективной передачи данных (Pipes).
- Правильно устанавливает `Content-Type` (например, `application/javascript`) для поддержки ES-модулей браузером.

### Утилиты (`lib/`)

#### FileManager.js (`lib/fileManager`)
Инструмент для работы с JSON файлами как с базой данных.
- **Атомарная запись:** Использует паттерн `writeFile` во временный файл -> `rename` в целевой файл. Это предотвращает повреждение данных при внезапном сбое записи.
- **Очистка мусора:** При ошибке операции автоматически удаляет временный файл (`.tmp-...`).
- **CRUD-методы:** `readJSON`, `writeJSON`, `findById`, `create`, `update`, `delete`.
- **Метаданные:** Автоматически генерирует `id` (UUID), `createdAt` и `updatedAt`.

#### DataGenerator.js (`lib/dataGenerator`)
Генератор синтетических данных для тестирования.
- Поддерживает типы: `string`, `number`, `boolean`, `date`, `array`, `object`.
- Позволяет задавать диапазоны (`min`, `max`) и глубину вложенности.

---

## 🎨 Компоненты архитектуры (Frontend)

**Фронтенд проекта полностью реализован на чистом (Vanilla) JavaScript без использования сторонних фреймворков (Vue, React, Angular).**

### Структура фронтенда (`public/assets/js/`)

#### app.js (`public/assets/js/app.js`)
Точка входа и инициализация приложения.
- **Инициализация Роутера:** Создает экземпляр класса `Router`.
- **Слушатели событий:** Подписывается на `hashchange` и `load` для навигации.
- **Инъекция иконок:** Заменяет заглушки `data-icon` в DOM на SVG из библиотеки.
- **Глобальные объекты:** Делает роутер (`window.router`) и UI-хелперы доступными глобально для обработчиков событий.

#### api.js (`public/assets/js/api`)
Класс `ApiClient` для взаимодействия с сервером.
- **Методы:** `get`, `post`, `put`, `delete`.
- **Управление токеном:** Автоматически сохраняет токен в `localStorage` и добавляет заголовок `Authorization: Bearer ...` к запросам.
- **Обработка ошибок:** Пробрасывает ошибки с сетевыми статусами (404, 500) в UI.

#### Router (`public/assets/js/` - внутри `app.js`)
Простой клиентский маршрутизатор для SPA.
- **Хеширование:** Использует `location.hash` для навигации (например, `#food`, `#admin`).
- **Вьюхи:** Асинхронные функции (Views), возвращающие строки HTML.
- **Перерисовка:** При смене хеша (`hashchange`) обновляет содержимое элемента `#app`.

#### views/ (`public/assets/js/views/`)
Модули, отвечающие за рендеринг HTML для разных страниц.
- **home.js:** Главная страница с карточками категорий.
- **catalog.js:** Страницы каталогов (еда, электроника, алкоголь, философия).
- **admin.js:** Страница панели администратора.
- **auth.js:** Страница входа.

#### adminLogic.js (`public/assets/js/adminLogic`)
Бизнес-логика управления админ-панелью.
- **Формы:** Функции `AdminForm.open`, `AdminForm.submit` для создания и редактирования товаров.
- **Парсинг:** Преобразует данные из HTML форм (`FormData`) в JSON (строки в числа, строки в массивы через запятую).
- **Валидация:** Проверяет данные перед отправкой на сервер.
- **Управление:** Функция `deleteItem` и `logoutAdmin`.

#### components.js (`public/assets/js/components`)
Утилита для рендера карточек товаров.
- **Шаблонизация:** Функция `createCardHTML(item)` генерирует HTML карточки на основе данных товара.
- **Адаптивность:** Использует Flexbox (`display: flex; flex-direction: column; height: 100%`), чтобы контент растягивался, а футер ("В наличии") прижимался к низу.

---

## 🔄 Поток обработки запроса

### 1. Backend (HTTP Request Flow)

1.  **Прием запроса:**
    Нативный HTTP-сервер (`http.createServer`) принимает запрос и передает его в `App._handleRequest`.
2.  **Создание оберток:**
    Создаются экземпляры `Request` и `Response`. Выполняется начальный парсинг URL (pathname и query).
3.  **Выполнение Middleware (`_runMiddlewares`):**
    Фреймворк **последовательно** проходит по всем middleware, зарегистрированным через `app.use()`.
    - Если middleware вызывает `next()`, управление передается следующему middleware.
    - Если middleware отправляет ответ (через `res.send`, `res.json`) и **не вызывает** `next()`, цикл прерывается.
    - Если middleware вызывает `next(err)`, управление сразу передается в **Обработчик Ошибок** (пункт 7), минуя другие middleware и роутер.
4.  **Поиск маршрута:**
    Если ответ не был отправлен, `Router` ищет совпадение по `method` и `path`.
    - Если маршрут найден, возвращается объект `{ handler, params, query }`.
    - Если маршрут не найден, срабатывает встроенная логика 404 (отправка текста "Маршрут не найден" или вызов кастомного обработчика, если настроен).
5.  **Извлечение параметров:**
    В объект `Request` мержатся параметры: `req.params = route.params`, `req.query = { ...req.query, ...route.query }`.
6.  **Выполнение обработчика маршрута:**
    Вызывается функция handler, привязанная к маршруту.
    - Если внутри handler происходит ошибка (`throw`) или вызывается `next(err)`, управление переходит в обработку ошибок.
7.  **Обработка ошибок (`_handleError`):**
    - Если в `App` был установлен кастомный обработчик (`setErrorHandler`), он вызывается с объектом ошибки. Именно он отвечает за формирование JSON-ответа для клиента.
    - Если кастомный обработчик не установлен, используется fallback: логирование ошибки в консоль и отправка текстового ответа "Внутренняя ошибка сервера" (код 500).

### 2. Frontend & Backend Interaction Flow

1.  **Инициализация:**
    Браузер загружает `index.html`.
    `app.js` инициализирует роутер и слушает изменения хеша URL.
2.  **Навигация пользователя:**
    Пользователь кликает на ссылку (например, "/food").
    Браузер меняет URL хеш на `#food`.
3.  **Срабатывание роутера:**
    Frontend `Router` перехватывает событие `hashchange`.
    Определяет соответствующую Вьюху (`Views.food`).
4.  **Рендеринг (View):**
    Вьюха вызывает `api.get('/food')`.
    API-клиент делает `fetch` запрос к Backend.
    Backend возвращает массив JSON.
    Вьюха генерирует HTML-строку таблицы товаров.
    `app.js` вставляет HTML в элемент `#app`.
    `injectIcons` заменяет заглушки иконок на реальные SVG.
5.  **Действие пользователя (Админка):**
    Пользователь нажимает "Редактировать".
    `adminLogic.js` открывает модальное окно и заполняет форму данными.
    Пользователь меняет цену и нажимает "Сохранить".
    `adminLogic.js` парсит данные (строку "500" в число `500`) и вызывает `api.put('/food/id', data)`.
    Backend обновляет запись в JSON файле.
    `window.router.handleRoute()` обновляет страницу (перерисовывает таблицу).

---

## 🏛️ Принципы проектирования

### Минимализм и "Vanilla JS"
*   **Backend:** Запрещено использование сторонних фреймворков (Express, Koa, Nest.js) и готовых решений. Вся логика реализована "под капотом" с использованием базовых модулей Node.js (`http`, `fs`, `crypto`, `url`).
*   **Frontend:** Фронтенд написан на чистом JavaScript без использования Vue, React, Angular или jQuery. Это позволяет полностью контролировать DOM и рендеринг, избегая лишних зависимостей и "магии" фреймворков.

### Модульность и Переиспользование
*   **Backend:** Каждый модуль сервиса (`food`, `alcohol` и т.д.) изолирован в папке `blueprints`. Контроллеры, Сервисы и Роуты разделены.
*   **Frontend:** Код разбит на модули (`views/`, `components/`, `logic/`, `api/`), соединенные через ES6 `import/export`. Компоненты (например, иконки, UI) переиспользуются.

### Безопасность данных (Atomicity)
*   **Backend:** Использование атомарных операций записи в `FileManager` (через временные файлы) исключает риск создания "битых" JSON-файлов при внезапном сбое питания или ошибке диска во время записи.
*   **Frontend:** API-клиент корректно обрабатывает токены (`Bearer`) и ошибки HTTP. Роутер защищает админку перенаправлением на логин при отсутствии токена.

### Явное управление потоком
Фреймворк (Backend) явно разделяет этапы (сначала middleware, потом роутинг, потом хендлеры), что делает поведение кода предсказуемым и прозрачным для разработчика.
Frontend-роутер также явно управляет жизненным циклом страницы (через `hashchange`), обеспечивая SPA-эффект без сложной библиотеки.
