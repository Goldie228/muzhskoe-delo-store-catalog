
# 🏗️ Архитектура фреймворка

## 📋 Обзор

Фреймворк построен на модульной архитектуре с четким разделением ответственности. Он предоставляет минимальный, но мощный набор инструментов для создания RESTful API без использования сторонних библиотек.

## 🧩 Компоненты архитектуры

### Ядро фреймворка (core/)

#### App.js
Центральный компонент фреймворка, который:
- Управляет жизненным циклом приложения
- Регистрирует middleware и маршруты
- Обрабатывает входящие HTTP-запросы
- Интегрирует все компоненты в единое целое

```javascript
const App = require('./core/App');
const app = new App();

// Регистрация middleware
app.use(bodyParser());

// Регистрация маршрутов
app.get('/api/users', handler);

// Запуск сервера
app.listen(3000);
```

#### Router.js
Мощный маршрутизатор с поддержкой:
- Параметров в пути (`/users/:id`)
- Query-строк (`/search?q=test`)
- Приоритетов маршрутов
- Автоматического извлечения параметров

```javascript
const router = new Router();
router.register('GET', '/users/:id', handler);
const match = router.find('GET', '/users/123');
// match.params = { id: '123' }
```

#### Request.js & Response.js
Расширенные объекты HTTP-запроса и ответа:
- Удобный доступ к параметрам и телу запроса
- Методы для отправки различных форматов ответов
- Поддержка цепочек вызовов

```javascript
// Request
console.log(req.params.id);
console.log(req.query.page);
const body = await req.parseBody();

// Response
res.status(201).json({ success: true, data: result });
```

#### Middleware (middleware/)

##### bodyParser.js
Промежуточный обработчик для парсинга тела запроса:
- Поддержка JSON и URL-encoded форматов
- Защита от слишком больших запросов
- Автоматическое определение типа контента

##### errorHandler.js
Централизованный обработчик ошибок:
- Логирование ошибок с контекстом
- Безопасные ответы для клиентов
- Различное поведение в development и production

### Утилиты (lib/)

#### fileManager.js
Инструмент для работы с JSON файлами:
- Атомарные операции чтения/записи
- CRUD-операции над коллекциями
- Автоматическая генерация ID и метаданных

#### dataGenerator.js
Генератор тестовых данных:
- Поддержка различных типов данных
- Вложенные схемы
- Настройка диапазонов и ограничений

## 🔄 Поток обработки запроса

1. **Прием запроса**: HTTP-сервер принимает запрос
2. **Создание объектов**: Request и Response оборачивают нативные объекты
3. **Выполнение middleware**: Последовательно выполняются все middleware
4. **Поиск маршрута**: Router ищет соответствующий маршрут
5. **Извлечение параметров**: Параметры пути и query-строки извлекаются
6. **Выполнение обработчика**: Вызывается функция обработчика маршрута
7. **Отправка ответа**: Ответ отправляется клиенту
8. **Обработка ошибок**: Любые ошибки перехватываются и обрабатываются

## 🏛️ Принципы проектирования

### Минимализм
Фреймворк предоставляет только необходимый функционал, избегая избыточности.

### Модульность
Каждый компонент имеет четкую зону ответственности и может использоваться независимо.

### Расширяемость
Архитектура позволяет легко добавлять новый функционал через middleware и плагины.

### Производительность
Оптимизированная обработка запросов с минимальными накладными расходами.

### Безопасность
Встроенная защита от основных уязвимостей и атак.
```
