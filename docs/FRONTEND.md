
# 🎨 Документация Фронтенда

## 📋 Обзор

Фронтенд проекта **"Мужское дело"** представляет собой современное SPA (Single Page Application), разработанное на **"Ванильном" JavaScript (Vanilla JS)** без использования сторонних фреймворков (React, Vue, Angular, jQuery).

Основной особенностью является полная модульность кода и разделение ответственности:
*   **Client Routing:** Собственный роутер, работающий через `window.location.hash`.
*   **API Layer:** Абстракция над `fetch` для работы с сервером (токены, обработка ошибок).
*   **Component System:** Переиспользуемые компоненты для рендера карточек товаров.
*   **Admin Panel:** Полноценная панель управления с динамическими формами и модальными окнами, реализованная как SPA-страница.

---

## 🏗 Архитектура Фронтенда

### Стек технологий
*   **HTML5:** Семантическая верстка.
*   **ES6 Modules:** `import/export` для разделения файлов.
*   **CSS3:** Вертикальные переменные, Flexbox, Grid для адаптивного дизайна.
*   **Fetch API:** Нативный браузерный API для HTTP-запросов.

### Организация кода

Все фронтендные файлы находятся в папке `public/assets/js/`.

```
public/assets/js/
├── app.js           # Точка входа, инициализация роутера
├── api.js           # API-клиент (GET, POST, PUT, DELETE)
├── config.js        # Конфигурация форм админки
├── ui.js            # UI-хелперы (Toasts, Modals)
├── icons.js         # Библиотека SVG иконок
├── adminLogic.js    # Логика админки (CRUD, Формы)
├── components.js     # Переиспользуемые компоненты (Карточки)
├── views/           # Вьюхи (Страницы сайта и Админки)
│   ├── index.js      # Склейщик вьюх
│   ├── home.js       # Главная
│   ├── catalog.js     # Каталог (Еда, Электроника...)
│   ├── admin.js       # Панель администратора
│   ├── auth.js        # Форма входа
│   └── info.js        # Страница "О нас"
└── (CSS файлы в ../css/)
```

---

## 🧩 Основные модули

### 1. `app.js` (Точка входа)

Главный файл, который запускает приложение.
*   **Инициализация Роутера:** Создает экземпляр класса `Router`, регистрирует маршруты (например, `#/food` -> `Views.food`).
*   **Глобальные объекты:** Делает роутер доступным через `window.router`, чтобы он был доступен в других модулях (например, в `adminLogic.js`).
*   **Инъекция Иконок:** Ищет заглушки `<span data-icon="home"></span>` в DOM и заменяет их на SVG из `icons.js`.
*   **Слушатели:** Следит за событиями `hashchange` и `load`, чтобы переключать страницы.

**Пример регистрации маршрута:**
```javascript
router.addRoute('food', Views.food); // URL #food -> Renders Views.food()
```

### 2. `api.js` (Клиент API)

Обертка над нативным `fetch`.
*   **Управление Токеном:** Автоматически добавляет токен из `localStorage` в заголовок `Authorization: Bearer <token>`.
*   **Методы:** Реализует `get`, `post`, `put`, `delete`.
*   **Обработка ошибок:** Если сервер вернет код статуса `!ok` (например, 400 или 500), выбрасывается `Error` с текстом ответа.

**Пример использования:**
```javascript
import { api } from './api.js';

const dishes = await api.get('/api/food');
const newDish = await api.post('/api/food', { name: 'Суп', price: 10 });
```

### 3. `config.js` (Админ-конфиг)

Содержит метаданные для полей форм в админ-панели (`ADMIN_CONFIG`).
*   Определяет список полей (`fields`) для каждого модуля (`food`, `electronics`, `alcohol`, `philosophy`).
*   Используется в `adminLogic.js` для генерации HTML форм.

**Пример конфигурации:**
```javascript
food: {
    label: 'Блюда',
    endpoint: '/api/food',
    fields: [
        { key: 'name', label: 'Название блюда', type: 'text' },
        { key: 'price', label: 'Цена (BYN)', type: 'number' }
    ]
}
```

### 4. `adminLogic.js` (Логика Админки)

Самый сложный модуль фронтенда. Отвечает за:
*   **Открытие модальных окон:** `AdminForm.open(mode, item, moduleKey)`. Рендерит форму.
*   **Парсинг данных:** `AdminForm.submit(...)` читает данные из HTML формы.
    *   **Числа:** Преобразует строки в `parseFloat` (важно для цены, объема).
    *   **Массивы:** Преобразует строки через запятую в массив (ингредиенты, теги).
*   **Отправка на сервер:** Вызывает методы `api` (POST для создания, PUT для редактирования).
*   **Обновление UI:** После успешной операции вызывает `window.router.handleRoute()`, чтобы перерисовать таблицу без перезагрузки страницы.

### 5. `views/admin.js` (Страница Админки)
*   **Сайдбар:** Меню выбора модуля.
*   **Таблица:** Рендерит список товаров с кнопками "Редактировать" и "Удалить".
*   **Пагинация:** Не реализована, но предусмотрена архитектурно.

### 6. `components.js` (Компоненты)
*   **`createCardHTML(item)`:** Генерирует HTML карточки товара.
    *   Распознает типы данных (дата, массив, объект) и красиво их оформляет.
    *   Использует Flexbox (`display: flex; flex-direction: column`), чтобы карточка растягивалась по высоте.

---

## 🎨 Стили и Верстка (CSS)

Стили находятся в `public/assets/css/`.
*   **CSS Переменные:** Определены в `_variables.css`. Используются для цветов, шрифтов, отступов.
*   **Grid System:** Используется CSS Grid для сетки карточек (`items-grid`).
*   **Admin Layout:** Админка использует Flexbox: `sidebar` + `content`.
    *   Боковая панель имеет фиксированную ширину.
    *   Контент занимает все доступное место (`flex-grow: 1`).

---

## 🧭 Панель Администратора (Подробно)

### 1. Работа с формами

Поля в админке генерируются динамически из `ADMIN_CONFIG`.
*   **Числовые поля:** (Цена, Напряжение) Вводятся как `<input type="number">`. `adminLogic.js` парсит их в `parseFloat` перед отправкой.
*   **Чекбоксы:** (В наличии) Возвращают `true`/`false`.
*   **Массивы:** (Ингредиенты) Вводятся как `"ингр1, ингр2"`. `adminLogic.js` разбивает их обратно в массив `['ингр1', 'ингр2']`.

### 2. Мгновенное обновление (SPA)

При нажатии кнопок "Сохранить", "Удалить" или "Выйти":
1.  Выполняется API-запрос.
2.  Если успех — вызывается `window.router.handleRoute()`.
3.  **Важно:** Страница не перезагружается (`window.location.reload`). Роутер заново вызывает функцию Вьюхи, получает данные и обновляет HTML внутри `<div id="app">`.

---

## 🧰 Как добавить новую страницу (Вьюху)

1.  Создай новый файл в `public/assets/js/views/` (например, `about.js`).
2.  Добавь в него функцию-экспорт:

```javascript
export const about = () => {
    return `<h1>О нас</h1><p>Мы лучшие!</p>`;
};
```

3.  Зарегистрируй вьюху в `app.js`:

```javascript
import { Views } from './views/index.js';

// ... внутри Router
router.addRoute('about', Views.about);
```

4.  Теперь URL `/#about` будет открывать твою страницу.

---

## 🧰 Как добавить иконку

1.  Открой `public/assets/js/icons.js`.
2.  Добавь строку SVG в объект `ICONS`:

```javascript
ICONS: {
    // ...
    phone: '<svg>...</svg>',
    // ...
}
```

3.  Используй в HTML (или вьюхе) заглушку с атрибутом:

```html
<span data-icon="phone"></span>
```

4.  Скрипт `app.js` (функция `injectIcons`) автоматически заменит заглушку на иконку.

---

## 🚀 Запуск фронтенда

Фронтенд работает на порту Node.js сервера.
1.  Запусти сервер: `npm start`.
2.  Открой браузер: `http://localhost:3000`.
3.  Сервер отдает `index.html`, который подтягивает `app.js`.

**Примечание:** При изменении CSS или JS файлов достаточно просто обновить страницу браузера (или перезагрузить `Ctrl+R`), так как сервер раздает их статикой (Hot Reload через `nodemon` может не быть настроен).
